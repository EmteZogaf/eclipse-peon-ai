# Plan — Fix Docs Lint Issues + Remove Redundant ADRs

## Context

Post-review lint findings across `docs/` — broken cross-links, missing registry entries, formatting inconsistency, wrong heading, and two ADRs that duplicate story content. Also moving two coding-pattern "ADRs" to their proper home in module AGENTS.md files where developers will actually see them.

**No code changes.** Pure docs housekeeping.

## Design decisions

| Decision | Why |
|----------|-----|
| Unify on `**Status** · Accepted` (newer style) | Matches ADR index table format, cleaner |
| Create `per-agent-think.md` story | ADR-0001/0003 link to it; think toggle exists in UI but has no docs home |
| Remove ADR-0012 + ADR-0013 | Content is verbatim in `scaffold-agent.md` R1/R2 — pure duplication |
| Keep ADR-0006 + ADR-0014 as ADRs | Broad sweep reads index.md anyway; no need to bloat AGENTS.md with duplicates |

## Affected files

### Create
| File | Change |
|------|--------|
| `docs/per-agent-think.md` | **New** — story home for think toggle (goal + 3 rules from ADRs) |

### Modify
| File | Change |
|------|--------|
| `docs/index.md` | Add `model-loading.md`, `per-agent-think.md`, `queued-user-messages.md` to Stories |
| `docs/adr/index.md` | Remove entries for 0012, 0013 (deleted as duplicates) |
| `docs/scaffold-agent.md` | Merge ADR-0012 + ADR-0013 details into R1/R2 inline |
| `docs/adr/0001-per-agent-think-string.md` | Unify status format; cross-link to new story ✅ |
| `docs/adr/0002-model-mapping-resource-files.md` | Unify status format |
| `docs/adr/0003-send-thinking-independent.md` | Unify status format |
| `docs/adr/0004-session-token-accounting.md` | Unify status format |
| `docs/adr/0006-swt-reflow-parent-on-size-change.md` | Unify status format (keep as ADR) |
| `docs/adr/0007-scaffold-agent-built-in.md` | Unify status format |
| `docs/adr/0008-aiagent-gettoolservice-routing.md` | Unify status format |
| `docs/adr/0009-reloadtool-dedicated.md` | Unify status format |
| `docs/adr/0010-standing-orders-setactiveagent-hook.md` | Unify status format |
| `docs/adr/0011-agent-template-system-prompt.md` | Unify status format |
| `docs/adr/0014-system-line-separator-in-llm-strings.md` | Fix heading typo; unify status format; **shorten** to 3-4 lines (rule + consequence, drop verbose context) |
| `docs/adr/0015-eclipse-sandbox-boundary.md` | Fix trailing bullet; unify status format |

### Delete
| File | Reason |
|------|--------|
| `docs/adr/0012-toolservice-boolean-constructor.md` | Duplicate of scaffold-agent.md R2 |
| `docs/adr/0013-persistent-agents.md` | Duplicate of scaffold-agent.md R1 |

## Implementation steps

### Step 1: Create `docs/per-agent-think.md`

```markdown
# Per-Agent Think Toggle

## Goal

Allow each agent to resolve its own effective think value per request — boolean-ish (`true`/`false`), concrete level (`high`/`medium`), or omitted. The 🧠 toggle in the action bar sets a session-only preference; per-agent overrides come from AGENT.md frontmatter or provider/model mapping files.

## Business Rules

### R1: Per-agent think resolution ✅
Each agent resolves its own effective think String per request via `ThinkResolver`. The value is computed fresh, never inherited across agents.

- **GIVEN** Peon-Plan has `think: high` in config **WHEN** it makes a request **THEN** the request carries `reasoning.effort=high` (OpenAI) or equivalent for the provider
- **GIVEN** Peon-Dev has think off **WHEN** it makes a request **THEN** no think attribute is sent

### R2: Provider/model mapping in resource files ✅
Auto mode translates generic "on" into provider+model-specific values via `resources/thinking/<PROVIDER>` files. Line format `pattern | on | off`. First match wins; no match → omit.

- **GIVEN** an OpenAI reasoning model matches a pattern **WHEN** auto mode resolves think **THEN** it returns the mapped value (e.g. `high`)
- **GIVEN** an unknown gateway model has no mapping **WHEN** auto mode resolves think **THEN** the attribute is omitted

### R3: think_send is global build-time preference ✅
`sendThinking` / `returnThinking` are langchain4j build-time switches — single global pref. Independent of per-agent think toggle.

- **GIVEN** `think_send` is enabled globally **WHEN** any agent calls a reasoning model **THEN** prior thinking is resent on the next turn

## ADRs
- [ADR-0001](adr/0001-per-agent-think-string.md) — Think resolved to per-agent String
- [ADR-0002](adr/0002-model-mapping-resource-files.md) — Mapping in resource files
- [ADR-0003](adr/0003-send-thinking-independent.md) — think_send independent of toggle

## Non-goals
- Per-agent `think_send` (reserved, blocked by langchain4j build-time limitation)
```

### Step 2: Update `docs/index.md` — add 3 missing stories

Add to Stories section (insert at logical positions):

```markdown
* [Model Loading](model-loading.md) - model list lifecycle: lazy fetch, persistence across agent switches, fallback on failure.
* [Per-Agent Think Toggle](per-agent-think.md) - per-request think resolution via provider mapping files and AGENT.md frontmatter.
* [Queued User Messages](queued-user-messages.md) - input queue with batching, FIFO consumption, drain-to-memory on abort.
```

### Step 3: Merge ADR-0012 + ADR-0013 into `docs/scaffold-agent.md`

**R1 addition** (from ADR-0013):
> Registered as a persistent agent via `addPersistentAgent()` — stored in AgentService's separate `persistentAgents` map, survives `clearAgents()` on reload.

**R2 addition** (from ADR-0012):
> Uses `ToolService(false)` constructor to start with an empty registry — prevents default-tool leakage into scaffold agent. The no-arg constructor delegates to `this(true)`.

### Step 4: Delete redundant ADR files

Remove from disk:
- `docs/adr/0006-swt-reflow-parent-on-size-change.md`
- `docs/adr/0012-toolservice-boolean-constructor.md`
- `docs/adr/0013-persistent-agents.md`
- `docs/adr/0014-system-line-separator-in-llm-strings.md`

### Step 5: Update `docs/adr/index.md` — remove deleted entries

Remove these rows from the table:
| [0012](0012-toolservice-boolean-constructor.md) | ToolService(boolean withDefaults) constructor | Accepted |
| [0013](0013-persistent-agents.md) | Persistent agents in AgentService survive clearAgents | Accepted |

### Step 6: Unify status format on all ADRs

Replace `**Status:** Accepted` → `**Status** · Accepted` in all surviving ADRs (0001–0005, 0007–0011, 0014–0015). Also fix the heading typo in ADR-0014:
- `# ADR-0007: Use System.lineSeparator()...` → `# ADR 0014 — Use System.lineSeparator() in Strings Sent to LLM`

### Step 7: Fix trailing bullet in `docs/adr/0015-eclipse-sandbox-boundary.md`

Ensure all three consequence bullets are complete with proper formatting:

```markdown
**Consequences** ·
- When disk tools are disabled, the agent is confined to Eclipse workspace files only — no risk of writing outside projects.
- The scaffold agent uses config-scoped disk tools as an exception — it needs to write to the `.peon` config directory which is outside the Eclipse workspace.
- Both tool sets should have symmetric behavior (same operations, same semantics) so the agent doesn't need to reason about which tool to use based on behavioral differences.
```

## BDD Use Cases

N/A — docs-only changes. No test code affected.

## Open questions

None — all fixes are deterministic lint corrections and redundancy removals.
